/**
 * The version this build calls itself — counterpart to PHP's `CliPackage::VERSION`.
 * Bumped with the release; `release-version.test.ts` holds it to the changelog.
 */
export const CLI_VERSION = '0.15.0';
